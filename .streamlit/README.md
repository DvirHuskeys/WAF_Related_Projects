# `.streamlit/`

Streamlit reads configuration values from this folder (e.g., `config.toml`).
Use it to keep UI theming, server defaults, and shared settings versioned so a
fresh clone matches the documented experience.
