# `ui/`

Contains the Streamlit entrypoint (`app.py`) and any supporting view helpers.
Keep layout decisions aligned with the UX specification under `docs/` and note
any experimental components or styling hooks here so other contributors can
find them quickly.
