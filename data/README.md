# `data/`

Holds the DuckDB warehouse (`warehouse.db`), sample CSV inputs, and vendor rule
exports. Everything under this directory stays local-only; do not check in
customer data. Use `.gitkeep` to ensure the folder exists on fresh clones.
